# winpc-mcp 依赖
# 跨平台核心
# fastmcp 固定在 3.4.x：其依赖的 MCP SDK 版本中 FastMCP 默认 host 为 localhost
# 时会自动启用 DNS rebinding 防护（421 Invalid Host header，拒绝局域网 IP 直连）。
# server.py 已显式关闭防护，此处固定版本保证行为可预期
fastmcp>=3.4,<3.5
fastapi>=0.110
uvicorn>=0.29
psutil>=5.9
requests>=2.31

# Windows 专属（pyautogui/mss/pyperclip 在非 Windows 平台也允许安装，
# 但工具在非 Windows 上调用时返回明确错误）
pyautogui>=0.9.54
mss>=9.0
pyperclip>=1.8
pillow>=10.0
